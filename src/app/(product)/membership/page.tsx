import { productIdentity, productMembership } from "@/server/services/product";
export default async function Membership(){const user=await productIdentity();const data=await productMembership(user.id);return <><div className="page-heading"><p className="eyebrow">PRODUCT PREVIEW</p><h1>Membership</h1><p>Useful learning comes first. Membership options are still being developed.</p></div><section className="product-card"><h2>Your account: {data.current.name}</h2><p>{data.current.voice}</p><p>{data.current.ads}</p><p className="callout">{data.current.paymentNotice}</p></section><div className="product-grid">{data.plans.map(p=><section className="product-card" key={p.name}><span className="status-chip">{p.preview?"Future options":"Available now"}</span><h2>{p.name}</h2><p>{p.description}</p><ul className="feature-list">{p.features.map(f=><li key={f.label}>{f.label}<span>{f.available?"Available":"Planned · not available yet"}</span></li>)}</ul>{p.preview&&<p>Pricing has not been announced. There is no checkout.</p>}</section>)}</div><section className="product-card"><h2>Future feature eligibility</h2><p>Eligibility reflects your account settings. It does not mean these future features have launched.</p><ul>{data.current.future.map(f=><li key={f.label}>{f.label}: {f.eligible?"eligible when available":"not included in current access"} · not available yet</li>)}</ul><p>Membership never changes what the teaching engine thinks you need to practise.</p></section></>;}
